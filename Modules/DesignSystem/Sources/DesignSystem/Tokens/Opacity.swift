/// Opacity tokens — no opacity literal escapes a view (CLAUDE.md rule 5).
public enum Opacity {
    /// The locked-medal ghost opacity, tuned against the mock (ADR-0007).
    public static let ghosted: Double = 0.35
}
